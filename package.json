{
  "name": "sidebarrer",
  "version": "0.1.0",
  "description": "Manually reorder files and folders in the Obsidian file explorer",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production"
  },
  "dependencies": {
    "monkey-around": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.3.0"
  }
}
